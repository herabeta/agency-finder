export async function personalizeAgency(input:{agency:string;city:string;services:string[];channel:'Email'|'WhatsApp'}){ const key=process.env.OPENAI_API_KEY; if(!key) return {configured:false,message:`Hi ${input.agency} team, I came across your travel services in ${input.city}. I would like to explore a partnership around ${input.services.slice(0,2).join(' and ')}. Would you be open to a quick conversation?`}; const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${key}`},body:JSON.stringify({model:process.env.OPENAI_MODEL||'gpt-5-mini',input:`Write a concise professional ${input.channel} outreach message for travel agency ${input.agency} in ${input.city}. Services: ${input.services.join(', ')}. Focus on partnership value, no unsupported claims.`})}); if(!r.ok) throw new Error('AI provider request failed'); const data=await r.json(); return {configured:true,message:data.output_text||''}; }
